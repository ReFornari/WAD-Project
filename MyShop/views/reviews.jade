extends layout
block content
  .row
    .container
      h1 Reviews page!
        .grid-container
            each review in reviews
                .item
                    h1.itemtitle #{review.name}
                    p.itemp #{review.content}
                    a.button(href="/editreviews/#{review.id}") Edit #{review.Name}
                    a.button(href="/deletereview/#{review.id}") Delete #{review.id}
                    